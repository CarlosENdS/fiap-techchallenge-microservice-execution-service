apiVersion: v1
kind: Service
metadata:
  name: execution-service
  namespace: execution-service
  labels:
    app: execution-service
spec:
  selector:
    app: execution-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
